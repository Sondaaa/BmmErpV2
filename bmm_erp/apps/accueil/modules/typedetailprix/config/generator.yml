generator:
  class: sfDoctrineGenerator
  param:
    model_class:           typedetailprix
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          typedetailprix
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
          libelle: {label: "Libellé"}
      list:
          title: Liste des Types détails prix
          display: [libelle]
          max_per_page: 5
      filter:  ~
      form:    ~
      edit:
          title: Modifier %%libelle%%
      new:
          title: Nouveau type détails prix
